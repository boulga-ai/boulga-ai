# BOULGA — Prompts Claude Code
## Première version — Focus Hub LLM
### 17 prompts + Prompt 0 (contexte)

Version 3.0 — Juin 2026

---

## Mode d'emploi

1. **Commence par le Prompt 0** — il crée le fichier CLAUDE.md que Claude Code relit automatiquement à chaque session.
2. **Donne les prompts dans l'ordre** — chaque prompt dépend des précédents.
3. **Vérifie les critères de succès** avant de passer au prompt suivant.
4. **Committe après chaque prompt réussi** — dis à Claude Code « commit avec un message clair ».
5. **Si un prompt dérive** — dis « relis CLAUDE.md et corrige X ».
6. **Ne saute pas de prompt** — même si un prompt te semble simple, il pose des bases pour les suivants.

---

## PROMPT 0 — Fichier de contexte (CLAUDE.md)

```
Crée un fichier CLAUDE.md à la racine du projet avec le contenu suivant. Ce fichier est ta référence permanente — relis-le à chaque session.

# BOULGA — Contexte du projet

## Vision
Boulga (« puits » en mooré) est une plateforme IA à double mission :

**Mission 1 — Centralisation :** Regrouper les grands LLM (Gemini, Claude, ChatGPT, DeepSeek) en une seule interface unifiée pour permettre aux utilisateurs de profiter des forces de chaque modèle selon leur besoin — sans jongler entre plusieurs outils, comptes et abonnements. Chaque LLM a ses avantages ; Boulga les met tous à portée de main.

**Mission 2 — Accessibilité :** Rendre ces LLM accessibles là où les moyens de paiement internationaux (cartes Visa/Mastercard étrangères) et la barrière de la langue excluent les utilisateurs. Paiement en mobile money local (CinetPay/FCFA), interface en français, support adapté aux réalités locales. Le marché de lancement est l'Afrique de l'Ouest francophone ; la vision est mondiale.

Ces deux missions se renforcent : la centralisation apporte de la valeur à tous les utilisateurs ; l'accessibilité ouvre le marché à ceux qui en étaient exclus.

La plateforme a 3 sections : Hub LLM (chat multi-modèle — FOCUS de cette version), Marketplace d'Agents (B2B — à venir), Academy IA (formations — à venir). Cette première version construit la plateforme complète avec le Hub LLM entièrement fonctionnel.

Stratégie de lancement : La Marketplace d'Agents (section B2B future) et l'Academy n'apparaissent NULLE PART sur le site public (ni navigation, ni landing, ni footer). Elles seront annoncées uniquement au moment de leur lancement. L'objectif est de ne pas alerter les concurrents locaux sur la roadmap.

DISTINCTION IMPORTANTE sur les agents :
- Les "8 agents métier" sont une FONCTIONNALITÉ du plan Océan → visibles et détaillés sur la page /pricing uniquement.
- La "Marketplace d'Agents IA" est une future SECTION du produit → cachée, jamais mentionnée publiquement.
Un visiteur voit les agents comme un avantage du plan Océan, pas comme l'annonce d'un produit futur.

## Stack technique (NON négociable)
- Backend : FastAPI Python 3.12 (async)
- Frontend : Next.js 14 App Router + TypeScript strict
- Styling : Tailwind CSS
- État : Zustand
- Base de données : Supabase (PostgreSQL + Storage)
- Cache / quotas : Redis
- LLM : LiteLLM ou SDKs directs (google-genai pour Gemini). PAS google-generativeai (déprécié).
- Paiement : CinetPay (mobile money) + Stripe (international)
- Email : Resend
- Hébergement : Vercel (front) + Railway (back) + Cloudflare (CDN)
- Icônes : Tabler Icons (outline uniquement, 20px inline, 24px décoratif)

## Charte graphique (STRICTE)
Couleurs :
  Marine profond #0B1F3A — primaire, fonds, titres, texte principal
  Bleu Boulga #1565C0 — secondaire, actions, liens, CTA
  Fond neutre #F5F7FA — background interface
  Blanc #FFFFFF — surfaces, cards, inputs
  Texte secondaire #4A5568 — descriptions
  Texte tertiaire #94A3B8 — labels, placeholders
  Succès #2E7D32 — validation, paiement réussi
  Attention #F57C00 — quota proche, expiration
  Erreur #C62828 — échec, suppression
  Info #1565C0
  Ratio : 60% blanc/fond, 30% marine, 10% bleu
Typographie :
  Titres / H1 / display : DM Serif Display Regular 400 (Google Fonts)
  Interface / corps : DM Sans 300/400/500 (Google Fonts)
  Display 36px, H1 26px, H2 18px, Corps 15px, UI 13-14px, Caption 12px
Espacements : multiples de 4px (4, 8, 12, 16, 24, 48)
Rayons : inputs/badges 4px, boutons 8px, cards 12px, modales 16px, tags pill
Bordure par défaut : 0.5px solid #E0E4EC
Animations : 100ms rapide, 200ms ease-in-out standard, 350ms expressif
Voix de marque : directe, chaleureuse, concrète, sans jargon, inclusive.

## LLM et modèles (juin 2026)
Pour chaque LLM : un modèle économique (low) et un avancé (high).
- Gemini : gemini-2.5-flash (low), gemini-2.5-pro (high) — ACTIF
- Claude : claude-haiku-4-5 (low), claude-sonnet-4-6 (high) — à venir
- ChatGPT : gpt-5.5-instant (low), gpt-5.5-pro (high) — à venir
- DeepSeek : deepseek-v4-flash (low), deepseek-v4-pro (high) — à venir
Seul Gemini est actif au lancement. Les autres sont déclarés mais inactifs.

## Features différenciantes (avantages exclusifs Boulga)

### Mode Comparaison
L'utilisateur envoie un message et reçoit les réponses de plusieurs LLM côte à côte simultanément.
Il voit concrètement les différences entre Gemini, Claude, ChatGPT, DeepSeek sur la même question.
Disponible à partir de l'offre Source. Interface en colonnes (desktop) ou en onglets (mobile).

### Routage Automatique Intelligent
Boulga analyse la question et choisit automatiquement le LLM le plus adapté :
- Code / développement → DeepSeek V4
- Raisonnement / analyse / juridique → Claude Sonnet
- Créativité / rédaction → ChatGPT 5.5
- Fichiers longs / PDF / recherche → Gemini 2.5 Flash
- Questions générales → Gemini 2.5 Flash (défaut)
L'utilisateur peut activer/désactiver ce mode. Disponible à partir de Source.

### Support langues locales
Détection automatique de la langue de l'utilisateur.
Support prioritaire : Français, Anglais, Wolof, Dioula, Mooré, Bambara.
L'interface est en français. Les LLM répondent dans la langue détectée si supportée.

### Bot WhatsApp
Intégration WhatsApp Business API : l'utilisateur envoie un message à un numéro Boulga
et reçoit la réponse du LLM directement dans WhatsApp. Aucune app à installer.
Canal d'acquisition et de rétention prioritaire pour le marché africain.
Disponible à partir de Source.

### PWA (Progressive Web App)
L'application web est installable sur Android et iOS sans passer par les stores.
Fonctionne en mode dégradé sur connexion lente (importante en Afrique de l'Ouest).

## Abonnements (5 niveaux)
Monnaie : FCFA partout. Deux modes de facturation : mensuel ou annuel (2 mois offerts).

### Gratuit — 0 FCFA
- 10 messages/jour (≈ 300/mois)
- LLM : Gemini Flash uniquement
- Modèles : éco seulement
- Fichiers : non
- Agents : non
- Historique : 7 jours
- Sièges : 1
- Rôle : acquisition et test de la plateforme

### Goutte — 2 999 FCFA/mois | 29 990 FCFA/an
- 600 messages/mois
- LLM : Gemini + DeepSeek
- Modèles : éco seulement
- Fichiers : 3 uploads/mois
- Agents : non
- Historique : 3 mois
- Sièges : 1
- Rôle : entrée payante, étudiant, primo-utilisateur

### Source ⭐ (offre principale) — 5 999 FCFA/mois | 59 990 FCFA/an
- 800 messages/mois
- LLM : tous (Gemini, Claude, ChatGPT, DeepSeek)
- Modèles : éco + avancés
- Fichiers : 20 uploads/mois
- Mode Comparaison : oui
- Routage Automatique : oui
- Bot WhatsApp : oui
- Agents : non
- Historique : 1 an
- Sièges : 1
- Rôle : freelance, professionnel solo — majorité des revenus

### Fleuve — 9 999 FCFA/mois | 99 990 FCFA/an
- 2 000 messages/mois
- LLM : tous
- Modèles : tous (éco + avancés)
- Fichiers : 50 uploads/mois
- Mode Comparaison : oui
- Routage Automatique : oui
- Bot WhatsApp : oui
- Agents : 2 agents au choix parmi les 8
- Historique : illimité
- Sièges : 1
- Rôle : power user, manager, PME solo

### Océan — 29 999 FCFA/mois | 299 990 FCFA/an
- Messages : illimité (cap fair use 50M tokens/mois)
- LLM : tous
- Modèles : tous (éco + avancés)
- Fichiers : illimité
- Mode Comparaison : oui
- Routage Automatique : oui
- Bot WhatsApp : oui
- Agents : 8 agents métier inclus + création d'agents personnalisés
- Historique : illimité
- Sièges : 10
- API access : oui
- Tableau de bord admin : oui (usage par membre)
- Facturation annuelle avec reçu officiel : oui
- Support prioritaire (réponse < 24h) : oui
- Rôle : entreprise, équipe, B2B

## Programme de parrainage
Mécanisme : l'utilisateur A partage son lien unique → l'utilisateur B s'inscrit via ce lien
→ l'utilisateur B souscrit à n'importe quel abonnement payant
→ l'utilisateur A reçoit automatiquement 14 jours d'accès Goutte gratuit.

Règles :
- La récompense est déclenchée uniquement lors du PREMIER paiement réussi du filleul.
- Délai de sécurité : la récompense est accordée 7 jours après le paiement du filleul.
  Si le filleul demande un remboursement dans ces 7 jours → récompense annulée, jamais accordée.
- Récompense proportionnelle au tier souscrit par le filleul :
    Filleul souscrit Goutte  → parrain reçoit 14 jours Goutte
    Filleul souscrit Source  → parrain reçoit 1 mois Goutte
    Filleul souscrit Fleuve  → parrain reçoit 1 mois Source
    Filleul souscrit Océan   → parrain reçoit 1 mois Fleuve
- Si A est déjà sur un niveau supérieur à la récompense : les jours s'ajoutent à son expiration.
- Si A est sur Gratuit : il monte temporairement au niveau récompensé, puis retourne en Gratuit.
- Pas de limite de parrainages : chaque filleul payant = une récompense pour A.
- Anti-fraude : un utilisateur ne peut pas se parrainer lui-même (vérification email + IP + device).
- Un filleul ne peut être lié qu'à un seul parrain (premier lien de parrainage utilisé à l'inscription).

Canaux de partage (par ordre de priorité) :
1. WhatsApp — bouton "Partager sur WhatsApp" avec message pré-rédigé + lien
2. Copier le lien — pour tout autre canal (SMS, Facebook, email manuel)
3. Email direct — champ "Inviter par email" qui envoie un email depuis Boulga (Resend)
Notification de récompense à A : WhatsApp si compte lié, email sinon (fallback automatique).

Table DB : referrals (id, referrer_id FK users, referred_id FK users,
            status CHECK pending|completed, reward_granted_at, created_at)
Champ additionnel sur users : referral_code VARCHAR UNIQUE (généré à l'inscription).

## Agents métier (Océan uniquement)
8 agents pré-configurés pour les entreprises d'Afrique de l'Ouest :
1. Agent Service Client — répond aux questions clients en français, gère FAQ, escalade
2. Agent Comptable OHADA — comptabilité selon plan comptable OHADA, états financiers
3. Agent Facturation — génère devis et factures en FCFA, suit les paiements en retard
4. Agent RH — fiches de paie, contrats de travail, gestion congés selon droit local
5. Agent Marketing — contenu Facebook/WhatsApp/Instagram adapté au marché africain
6. Agent Reporting — génère rapports automatiques depuis données brutes (Excel, CSV)
7. Agent Juridique — contrats simples, conformité OHADA, modèles documents légaux
8. Agent Traducteur — Français ↔ Anglais + Wolof, Dioula, Mooré (si LLM supporté)
Fleuve : 2 agents au choix. Océan : les 8 + agents personnalisés illimités.

## Authentification
Inscription simple : email, nom complet, date de naissance, mot de passe.
Connexion : email + mot de passe → JWT en cookie httpOnly.

## Conventions de code
- Séparation stricte : routers → services → managers → repositories. Jamais de logique métier dans les routers.
- Noms de fichiers et variables en anglais ; commentaires et textes UI en français.
- Typage strict (Pydantic backend, TypeScript frontend).
- Secrets via .env, jamais dans le code.

## Structure des dossiers
boulga/
  backend/
    main.py
    app/
      config.py
      core/           (cors, exceptions, security, rate_limiter)
      db/             (session, schema.sql, repositories/)
      routers/        (auth, chat, conversations, files, llms, payments,
                       subscriptions, feedback, search, compare, whatsapp)
      schemas/        (auth, chat, compare, payment, subscription)
      services/       (auth_service, chat_service, file_service,
                       compare_service, routing_service, quota_service,
                       subscription_service, payment_service, whatsapp_service)
      manager/        (registry, llm_manager, providers/, router_agent)
      prompts/        (chat_prompts, tool_prompts, routing_prompts)
      utils/          (sse, languages, tokens)
  frontend/
    app/
      (layout, page)
      chat/           (page, [id]/page)
      auth/           (login, register, forgot)
      documents/      (page)
      settings/       (page)
      pricing/        (page)
      compare/        (page — Mode Comparaison)
    components/
      nav/            (Navigation)
      sidebar-left/   (ConversationList)
      chat/           (ChatWindow, MessageBubble, ChatInput, LLMSelector,
                       ComparePanel, RoutingIndicator)
      sidebar-right/  (DocumentPanel)
      ui/             (Button, Badge, Input, Card, Toast, Modal,
                       LangSelector, QuotaBadge)
    store/            (chatStore, authStore, docStore, compareStore)
    lib/              (api, stream, constants, languages)
    types/            (index — tous les types TypeScript)

## Ce qu'il NE faut PAS faire
- Ne pas ajouter de fonctionnalités non demandées.
- Ne pas utiliser localStorage/sessionStorage.
- Ne pas mettre de logique métier dans les routers.
- Ne pas coder les providers LLM autres que Gemini tant qu'on ne le demande pas.
- Ne pas inventer de clés API — utiliser les variables d'environnement.
- Ne pas hardcoder de sections de document (le LLM décide de la structure).
- Ne pas construire de convertisseur de fichiers côté backend — le LLM génère les fichiers.
- Ne jamais mentionner la Marketplace d'Agents ou l'Academy sur aucune page publique — ni nav, ni landing, ni footer, ni metadata SEO.
- Les 8 agents métier du plan Océan sont mentionnés UNIQUEMENT sur la page /pricing comme feature du plan — jamais comme section ou produit séparé.
- Ne pas afficher "illimité" sans cap technique côté backend — toujours avoir un fair use limit.
- Ne pas laisser un utilisateur Océan dépasser 50M tokens/mois sans alerte admin.
- Ne pas appeler plusieurs LLM en parallèle (Mode Comparaison) sans gérer les timeouts individuellement.
- Ne pas exposer les coûts API réels à l'utilisateur — afficher seulement les quotas en messages/tokens.

Confirme que tu as créé CLAUDE.md et résume le projet en 5 lignes.
```

**Critère de succès :** CLAUDE.md existe à la racine ; Claude Code résume correctement.

---

## PHASE 1 — FONDATIONS

### PROMPT 1 — Setup projet + base de données + repositories

```
En te basant sur CLAUDE.md, initialise le projet Boulga complet.

BACKEND (boulga/backend/) :
1. Crée toute l'arborescence décrite dans CLAUDE.md avec les __init__.py.
2. requirements.txt : fastapi, uvicorn[standard], pydantic, pydantic-settings, google-genai, litellm, supabase, redis, python-dotenv, httpx, python-docx, openpyxl, bcrypt, pyjwt, resend. Versions récentes compatibles.
3. .env.example avec toutes les variables :
   GEMINI_API_KEY, SUPABASE_URL, SUPABASE_KEY, REDIS_URL,
   JWT_SECRET, JWT_REFRESH_SECRET,
   RESEND_API_KEY,
   CINETPAY_API_KEY, CINETPAY_SITE_ID, CINETPAY_SECRET,
   WHATSAPP_TOKEN, WHATSAPP_PHONE_ID, WHATSAPP_VERIFY_TOKEN, META_APP_SECRET,
   CORS_ORIGINS, FRONTEND_URL, APP_ENV (development|production)
4. .gitignore (venv, .env, __pycache__, node_modules, .next).
5. app/config.py : Pydantic Settings chargeant le .env.
6. main.py : app FastAPI avec GET / (nom + version) et GET /health.
7. app/core/cors.py : CORS depuis CORS_ORIGINS.
8. app/core/exceptions.py : exception BoulgaError + handlers.
9. app/core/security.py : fonctions hash_password, verify_password (bcrypt), create_jwt, decode_jwt. Dépendance FastAPI get_current_user (optionnelle, levée HTTPException 401 si pas de token).

BASE DE DONNÉES :
10. app/db/schema.sql — le schéma complet des 9 tables (copie exacte du cahier des charges) :
    users (id, email UNIQUE, name, date_of_birth, password_hash, email_verified, locale, created_at)
    conversations (id, user_id FK, title, created_at, updated_at) + INDEX updated_at DESC
    messages (id, conversation_id FK CASCADE, role CHECK, content, provider, model_id, file_ids jsonb, created_at) + INDEX conversation_id + created_at
    files (id, user_id FK, original_name, mime_type, storage_path, size_bytes, created_at)
    subscriptions (id, user_id FK, tier CHECK free|goutte|source|fleuve|ocean,
                   billing_cycle CHECK monthly|annual, status CHECK active|expired|cancelled,
                   started_at, expires_at, created_at)
    usage_quotas (id, user_id FK, period_start, period_end, messages_used DEFAULT 0,
                  files_generated DEFAULT 0, tokens_used DEFAULT 0, updated_at)
    payments (id, user_id FK, provider CHECK cinetpay|stripe, amount_fcfa,
              billing_cycle CHECK monthly|annual, status CHECK pending|success|failed,
              external_ref UNIQUE, created_at)
    feedback (id, user_id FK, message_id FK, rating CHECK up|down, comment, created_at)
    tool_prompts (id, slug UNIQUE, name, system_prompt, template_id, active, created_at)
    agents (id, slug UNIQUE, name, description, system_prompt, icon, category, active, created_at)
    user_agents (id, user_id FK, agent_id FK, assigned_at) + UNIQUE(user_id, agent_id)
    comparison_sessions (id, user_id FK, prompt TEXT, created_at)
    comparison_results (id, session_id FK CASCADE, provider, model_id, content TEXT,
                        tokens_used INT, latency_ms INT, created_at)
    whatsapp_sessions (id, user_id FK, phone_number UNIQUE, verified DEFAULT false,
                       last_activity, created_at)
    team_members (id, owner_user_id FK, member_user_id FK, role CHECK admin|member,
                  invited_at, joined_at) -- pour l'offre Océan (10 sièges)
    referrals (id, referrer_id FK users, referred_id FK users UNIQUE,
               status CHECK pending|completed|cancelled,
               reward_tier CHECK goutte|source|fleuve|ocean,
               reward_days INT,
               reward_due_at TIMESTAMPTZ,
               reward_granted_at TIMESTAMPTZ,
               created_at)
    + Champ referral_code VARCHAR(12) UNIQUE NOT NULL sur la table users
    + TRIGGER update conversations.updated_at on INSERT messages

11. app/db/session.py : client Supabase mis en cache.

REPOSITORIES (app/db/repositories/) :
12. Un fichier par entité — 15 repositories :
    user_repository, conversation_repository, message_repository,
    file_repository, subscription_repository, quota_repository,
    payment_repository, feedback_repository, tool_prompt_repository,
    agent_repository, user_agent_repository,
    comparison_repository, whatsapp_repository,
    team_repository, referral_repository.
    Chaque repository a les opérations CRUD de base + les requêtes spécifiques.

FRONTEND (boulga/frontend/) :
13. Initialise un projet Next.js 14 App Router + TypeScript strict.
14. Installe tailwindcss, @tabler/icons-react.
15. Crée les dossiers : app/, components/(nav, sidebar-left, chat, sidebar-right, ui), store/, lib/, types/.
16. Crée lib/constants.ts avec NEXT_PUBLIC_API_URL (défaut http://localhost:8000).
17. Crée types/index.ts avec tous les types :
    LLM, Model, Conversation, Message, User, File,
    Tier (free | goutte | source | fleuve | ocean),
    BillingCycle (monthly | annual),
    Subscription (tier, billing_cycle, status, expires_at),
    ComparisonSession, ComparisonResult,
    Agent, UserAgent,
    TeamMember,
    ReferralStats (total, completed, pending, days_earned),
    QuotaInfo (messages_remaining, files_remaining, tokens_remaining, period_end),
    WhatsAppSession.

NE code PAS encore de logique frontend (composants, pages). Juste la structure.
```

**Critères de succès :**
- `uvicorn main:app --reload` démarre, GET / et /health répondent
- `npm run dev` démarre côté frontend
- Le schéma SQL s'exécute sans erreur dans Supabase
- Chaque repository s'importe sans erreur
- security.py hash et vérifie un mot de passe, crée et décode un JWT
- Les types TypeScript compilent

---

### PROMPT 2 — Design system + layout global + navigation

```
En te basant sur CLAUDE.md (section charte graphique), configure le design system et construis la navigation globale de Boulga.

1. tailwind.config.ts — encode EXACTEMENT la charte :
   - Couleurs : marine (#0B1F3A), blue (#1565C0 avec nuances), neutral (#F5F7FA), white, textes (#0B1F3A, #4A5568, #94A3B8), success (#2E7D32), warning (#F57C00), error (#C62828)
   - Rayons : sm (4px), md (8px), lg (12px), xl (16px), full
   - Bordure par défaut : borderColor #E0E4EC
   - Toutes les tailles de police de la charte

2. app/globals.css — reset, variables CSS si nécessaire, styles de base.

3. Charge DM Serif Display et DM Sans via next/font/google dans le layout.

4. app/layout.tsx — layout racine :
   - Fond #F5F7FA
   - Polices appliquées
   - Structure : navigation + contenu principal

5. components/nav/Navigation.tsx — navigation globale de la plateforme :
   DESKTOP : sidebar fine (60px) à gauche avec icônes Tabler :
     - Logo Boulga en haut (texte "B" stylisé ou icône puits) — lien vers /
     - Hub LLM (icon: message-chatbot) — actif, lien vers /chat
     - Comparaison (icon: layout-columns) — actif Source+, lien vers /compare
     - Documents (icon: file-text) — lien vers /documents
     - Séparateur
     - Paramètres (icon: settings) — lien vers /settings
   MOBILE (< 768px) : barre d'onglets fixe en bas avec : Hub, Comparaison, Documents, Paramètres.
   L'élément actif est mis en évidence (couleur bleu Boulga).
   IMPORTANT : Aucune mention de Agents IA ou Academy nulle part dans la navigation.

6. components/ui/ — crée les composants de base réutilisables selon la charte :
   - Button.tsx (variantes : primary/secondary/ghost/destructive, tailles sm/md/lg)
   - Badge.tsx (variantes : pro/gratuit/actif/bientôt/expiré)
   - Input.tsx (avec label, erreur, placeholder)
   - Card.tsx (fond blanc, bordure, rayon 12px)
   - Toast.tsx (notification non bloquante, coin supérieur droit)
   - Modal.tsx (overlay, centré, rayon 16px)

Applique strictement CHAQUE token de la charte. Pas d'approximation.
```

**Critères de succès :**
- Les couleurs #0B1F3A et #1565C0 apparaissent correctement
- DM Sans (corps) et DM Serif Display (titres) se chargent
- La navigation sidebar est visible sur desktop, onglets en bas sur mobile
- Navigation contient exactement : Hub LLM, Comparaison, Documents, Paramètres — rien d'autre
- Aucune mention de Agents ou Academy dans la navigation ou le layout
- Les composants UI (Button, Badge, Input, Card) s'affichent selon la charte
- Le responsive fonctionne (tester 375px et 1440px)

---

### PROMPT 3 — Landing page + pages globales

```
En te basant sur CLAUDE.md, construis la landing page et les pages globales de Boulga.

1. app/page.tsx — LANDING PAGE publique :
   - Hero : titre en DM Serif Display ("Puisez l'intelligence qu'il vous faut" ou similaire),
     sous-titre en DM Sans, bouton CTA "Commencer gratuitement" (bleu Boulga).
   - Section "Tous les LLM en un seul endroit" : présenter Gemini, Claude, ChatGPT, DeepSeek
     avec leurs forces respectives. Gemini actif, les autres "Bientôt" sans détail de roadmap.
   - Section "Mode Comparaison" : présenter la feature côte à côte avec visuel illustratif.
   - Section "Accessible partout" : paiement mobile money, interface française, langues locales.
   - Section Pricing : toggle Mensuel/Annuel + 5 cards (Gratuit, Goutte, Source ⭐, Fleuve, Océan).
     Design exact de la charte. Source avec badge "Populaire".
   - Section "Inscrivez-vous pour les nouveautés" : champ email discret, texte vague
     "Boulga évolue constamment — soyez les premiers informés." Sans mentionner quoi.
   - Footer : liens (Accueil, Tarifs, Contact, Mentions légales), copyright boulga.ai.
   IMPORTANT : Aucune mention de Agents IA ou Academy. Aucune section "3 piliers".
   Aucune roadmap visible.

2. app/pricing/page.tsx — page dédiée aux tarifs :
   - Toggle Mensuel / Annuel en haut (2 mois offerts).
   - 5 cards avec tous les détails par plan :
     * Gratuit : 10 msg/jour, Gemini Flash, historique 7j
     * Goutte : 600 msg/mois, Gemini + DeepSeek, 3 fichiers, historique 3 mois
     * Source ⭐ : 800 msg/mois, tous LLM, Mode Comparaison, WhatsApp, 20 fichiers, 1 an
     * Fleuve : 2 000 msg/mois, tous LLM avancés, 2 agents métier au choix, 50 fichiers
     * Océan : illimité, tous LLM, 10 sièges, API, et la liste des 8 agents métier inclus :
         "Service client · Comptabilité OHADA · Facturation FCFA · RH · Marketing
          Reporting · Juridique · Traduction"
         Présentés comme features du plan, pas comme section produit séparée.
   - Afficher l'économie annuelle sur chaque card payante ("Économisez X FCFA").
   - Bouton "Commencer gratuitement" sur Gratuit → /auth/register.
   - Bouton "Choisir ce plan" sur les plans payants (non fonctionnel au prompt 3, branché au 13).
   - FAQ tarifaire en bas : remboursement, changement de plan, mobile money, facture, équipe.

3. app/documents/page.tsx — liste des fichiers :
   - État vide : "Aucun document pour l'instant. Vos fichiers générés et uploadés apparaîtront ici."
   - Structure prête pour la liste (nom, type, date, taille, bouton télécharger).

4. app/settings/page.tsx — paramètres :
   - Section Profil : nom, email (non modifiable), date de naissance. Bouton "Enregistrer".
   - Section Abonnement : niveau actuel (badge), cycle (mensuel/annuel), expiration,
     bouton "Changer de plan" → /pricing.
   - Section Quota : messages restants / total, fichiers restants / total. Barre de progression.
   - Section Parrainage : lien unique, bouton WhatsApp, compteur filleuls.
   - Section WhatsApp : liaison compte (visible Source+, grisé sinon).
   - Section Agents : visible Fleuve+.
   - Langue : sélecteur Français/English.
   - Bouton Déconnexion.
   Statique au prompt 3, branché avec les vraies données au prompt 12.

Toutes les pages utilisent le layout global avec la navigation épurée.
Toutes les pages sont responsives.
AUCUNE page /agents ou /academy dans cette version.
```

**Critères de succès :**
- La landing page s'affiche sur / avec hero, LLM, Mode Comparaison, pricing, footer
- Aucune mention de Agents IA ou Academy sur la landing, le footer ou les metadata
- Les 5 cards tarifaires sont conformes à la charte (Source = "Populaire")
- Toggle Mensuel/Annuel fonctionne sur /pricing, les prix s'affichent correctement
- Card Océan liste les 8 agents métier comme features du plan
- Les pages /documents et /settings sont accessibles depuis la navigation
- Aucune page /agents ou /academy n'existe dans le projet
- Le responsive fonctionne sur toutes les pages (375px mobile, 1440px desktop)
- DM Serif Display est utilisé pour les titres héros, DM Sans pour le reste

---

## PHASE 2 — CHAT BACKEND

### PROMPT 4 — Registre LLM + manager + Gemini + prompts

```
En te basant sur CLAUDE.md, construis le système LLM du backend.

1. app/manager/registry.py — SOURCE DE VÉRITÉ :
   - Enum LLMProvider : gemini, claude, openai, deepseek
   - Enum ModelTier : low, high
   - Modèles Pydantic : ModelInfo (id, label, tier, description), LLMInfo (provider, label, description, active, models)
   - Dict LLM_REGISTRY avec les 4 LLM et leurs 2 modèles (voir CLAUDE.md). Seul Gemini active=True.
   - Fonctions : get_all_llms(), resolve_model(provider, model_id), is_provider_active(provider),
     get_models_for_tier(tier) qui retourne les modèles autorisés par abonnement :
       free    → gemini-2.5-flash uniquement
       goutte  → gemini-2.5-flash + deepseek-v4-flash (modèles éco seulement)
       source  → tous les LLM, tous les modèles éco + avancés (low + high)
       fleuve  → tous les LLM, tous les modèles éco + avancés (identique Source)
       ocean   → tous les LLM, tous les modèles sans exception
     Note : Source et Fleuve ont le même accès modèles — la différence est dans les quotas,
     agents et fonctionnalités, pas dans les modèles accessibles.

2. app/manager/llm_manager.py — orchestration :
   - Utilise le SDK google-genai directement pour Gemini (client.aio.models.generate_content_stream pour le streaming async, rôles user/model).
   - Méthode stream_chat(provider, model_id, messages, system_prompt, files) → AsyncIterator[str] qui yield les chunks de texte.
   - Méthode generate_text(provider, model_id, prompt, system_prompt) → str pour les appels non-streaming (titre auto, etc.).
   - Vérifie que le provider est actif et le modèle existe avant chaque appel.
   - Gère les fichiers multimodaux (images, PDF) pour Gemini si présents.
   - Structure prête pour ajouter Claude/OpenAI/DeepSeek plus tard (juste ajouter un elif dans les méthodes).

3. app/prompts/chat_prompts.py :
   - DEFAULT_SYSTEM_PROMPT : le prompt Boulga exactement comme dans le cahier des charges (identité, fichiers, documents, règles). Court et ouvert.
   - TITLE_GENERATION_PROMPT : "Génère un titre de 3-5 mots pour cette conversation. Réponds uniquement avec le titre. Message : {message}"

4. app/prompts/tool_prompts.py :
   - Fonction get_tool_prompt(slug) qui lit le prompt spécialisé depuis la table tool_prompts en base.
   - Fonction get_full_system_prompt(tool_slug=None) qui combine le prompt de base + le prompt spécialisé si un outil est actif.

5. app/routers/llms.py — GET /api/llms :
   - Retourne la liste complète des LLM et modèles depuis le registre.
   - Format : [{ provider, label, description, active, models: [{ id, label, tier, description }] }]

6. app/schemas/chat.py — les schemas Pydantic :
   - LLMOut, ModelOut (sélecteur)
   - ChatRequest :
       conversation_id: str | None = None
       message: str
       provider: str = "gemini"
       model_id: str = "gemini-2.5-flash"
       file_ids: list[str] = []
       tool_slug: str | None = None
       auto_route: bool = False   ← Routage Automatique Intelligent
   - ConversationOut, MessageOut, ConversationDetailOut

Enregistre le router llms dans main.py.
NE code PAS les providers Claude/OpenAI/DeepSeek — juste les déclarer dans le registre.
```

**Critères de succès :**
- GET /api/llms retourne 4 LLM (1 actif, 3 "à venir") avec leurs modèles
- Un appel stream_chat avec Gemini retourne des chunks de texte réels
- Un appel generate_text retourne un texte complet
- Le prompt système est appliqué
- Un appel avec un provider inactif ou un modèle inconnu → erreur claire
- get_models_for_tier("free") retourne seulement gemini-flash et deepseek-flash

---

### PROMPT 5 — Chat streaming + conversations + historique

```
En te basant sur CLAUDE.md, construis le chat complet avec streaming.

1. app/services/chat_service.py — ChatService :
   stream_message(user_id, message, provider, model_id, conversation_id, file_ids, tool_slug) :
   C'est un générateur asynchrone qui yield des dicts (événements SSE).

   Flux exact :
   a. Si pas de conversation_id → créer la conversation (conversation_repository)
   b. yield {"type": "conversation", "id": conv_id, "is_new": bool}
   c. Vérifier le quota (quota_repository) → si épuisé, yield {"type": "error", "message": "Quota épuisé..."} et return
   d. Enregistrer le message utilisateur (message_repository)
   e. Récupérer l'historique complet de la conversation
   f. Si file_ids → récupérer les fichiers et préparer pour le LLM
   g. Assembler le system prompt (base + tool_prompt si tool_slug)
   h. GESTION DU CONTEXTE LONG : si l'historique dépasse 80% de la fenêtre du modèle,
      résumer les anciens messages via un appel rapide generate_text au modèle éco,
      garder les 5 derniers échanges intacts, injecter le résumé au début
   i. Streamer via llm_manager.stream_chat() :
      - Accumuler le texte complet
      - yield {"type": "chunk", "text": chunk} pour chaque morceau
   j. Si erreur LLM → yield {"type": "error", "message": str(e)} et return
   k. Enregistrer la réponse complète de l'assistant (message_repository)
   l. Incrémenter le quota messages (quota_repository)
   m. Si nouvelle conversation → générer le titre (generate_text + TITLE_PROMPT), sauver, yield {"type": "title", "title": t}
   n. yield {"type": "done", "message_id": saved_id}

2. app/utils/sse.py — fonction sse_event(data: dict) → str formaté "data: {json}\n\n"

3. app/routers/chat.py — POST /api/chat :
   - Reçoit ChatRequest
   - Récupère le user_id depuis le JWT (ou user anonyme temporaire si pas d'auth encore)
   - Retourne StreamingResponse text/event-stream avec headers Cache-Control no-cache, X-Accel-Buffering no
   - Consomme le service et formate chaque événement via sse_event

4. app/routers/conversations.py :
   - GET /api/conversations : liste triée par updated_at desc (ConversationOut[])
   - GET /api/conversations/{id} : conversation + messages (ConversationDetailOut), 404 si introuvable
   - DELETE /api/conversations/{id} : suppression cascade, 404 si introuvable

Enregistre les routers dans main.py.
```

**Critères de succès :**
- POST /api/chat streame une réponse Gemini token par token
- Les événements suivent le protocole : conversation → chunk(s) → title (si nouveau) → done
- Une nouvelle conversation a un titre auto-généré de 3-5 mots
- GET /api/conversations liste les conversations récentes
- GET /api/conversations/{id} retourne la conversation + ses messages dans l'ordre
- DELETE supprime la conversation et ses messages en cascade
- Si quota épuisé → événement error émis AVANT tout appel LLM
- Un historique de 50+ messages est géré (troncature/résumé si nécessaire)

---

### PROMPT 6 — Upload de fichiers + transmission au LLM

```
En te basant sur CLAUDE.md, ajoute l'upload et le traitement des fichiers.

1. app/routers/files.py :
   - POST /api/files/upload (multipart/form-data) :
     Reçoit un fichier, le stocke dans Supabase Storage, crée un enregistrement dans la table files, retourne { id, original_name, mime_type, size_bytes }.
     Limite : 50 MB max.

   - GET /api/files/{id}/download :
     Retourne le fichier depuis Supabase Storage. 404 si introuvable.

2. app/services/file_service.py — FileService :
   - store_file(user_id, uploaded_file) → enregistre dans Storage + base, retourne l'enregistrement.
   - get_file_content_for_llm(file_id) → selon le type :
     * CSV / TXT : lit le contenu texte. Si > 50 000 lignes, retourne headers + 20 premières lignes + profil (nb lignes, colonnes, types, valeurs manquantes).
     * PDF : si le LLM est Gemini ou GPT (multimodal), retourne le fichier en bytes pour envoi multimodal. Sinon, extrait le texte via PyMuPDF ou pdfplumber.
     * DOCX : extrait le texte via python-docx (paragraphes + tableaux).
     * XLSX : extrait en CSV par feuille via openpyxl (headers + données).
     * Image (PNG, JPG) : retourne les bytes pour envoi multimodal.
   - get_file_profile(file_id) → profil résumé pour les fichiers volumineux.

3. Intègre dans le ChatService :
   - Quand file_ids est présent dans la requête, appelle get_file_content_for_llm pour chaque fichier.
   - Ajoute le contenu des fichiers au contexte envoyé au LLM (texte dans le message, ou multimodal selon le type et le LLM).

4. Intègre dans le LLM Manager :
   - stream_chat accepte un paramètre files optionnel.
   - Pour Gemini : les images et PDF sont envoyés comme types.Part avec le bon mime_type.
   - Pour les fichiers texte : le contenu est injecté dans le message utilisateur.

Enregistre le router files dans main.py.
```

**Critères de succès :**
- Upload d'un CSV de 100 lignes → stocké, enregistré en base
- Envoyer un message avec le file_id du CSV → le LLM analyse les données et répond
- Upload d'un PDF → le LLM répond à des questions sur le contenu
- Upload d'une image → Gemini la décrit (vision)
- Upload d'un CSV de 60 000 lignes → le LLM reçoit un échantillon + profil, pas d'erreur
- GET /api/files/{id}/download → le fichier se télécharge
- Un fichier > 50 MB → erreur 413

---

## PHASE 3 — CHAT FRONTEND

### PROMPT 7 — Store Zustand + couche API + parsing SSE

```
En te basant sur CLAUDE.md, construis la couche de données du frontend.

1. frontend/store/chatStore.ts (Zustand) :
   État :
     conversations: Conversation[]
     currentConversationId: string | null
     messages: Message[]
     llms: LLM[]
     selectedProvider: string ("gemini")
     selectedModel: string ("gemini-2.5-flash")
     isStreaming: boolean
     streamingText: string
   Actions :
     loadLLMs() — appelle GET /api/llms
     loadConversations() — appelle GET /api/conversations
     loadConversation(id) — appelle GET /api/conversations/{id}, met à jour messages
     sendMessage(text, fileIds?) — appelle POST /api/chat en streaming :
       - Ajoute le message user à messages immédiatement (optimistic)
       - Crée une bulle assistant vide
       - Ouvre le flux SSE via streamChat()
       - À chaque chunk : concatène à streamingText
       - À title : met à jour le titre de la conversation dans conversations
       - À done : finalise le message assistant, reset streamingText et isStreaming
       - À error : affiche l'erreur, garde le message user
     stopStreaming() — interrompt le flux (abort controller)
     newConversation() — reset currentConversationId et messages
     deleteConversation(id) — appelle DELETE, met à jour la liste
     selectProvider(p), selectModel(m) — met à jour la sélection

2. frontend/store/authStore.ts (Zustand) :
   État : user: User | null, isAuthenticated: boolean
   Actions : login, logout, loadUser
   Pour l'instant, user est null et isAuthenticated est false. Sera branché au prompt 12.

3. frontend/lib/api.ts — appels typés vers le backend :
   - getLLMs(): Promise<LLM[]>
   - getConversations(): Promise<Conversation[]>
   - getConversation(id): Promise<ConversationDetail>
   - deleteConversation(id): Promise<void>
   - uploadFile(file: File): Promise<FileInfo>
   - downloadFile(id): Promise<Blob>
   Chaque fonction utilise fetch vers NEXT_PUBLIC_API_URL avec les bons headers.
   En cas d'erreur HTTP, throw avec le message d'erreur du backend.

4. frontend/lib/stream.ts — streamChat(payload, handlers) :
   - POST vers /api/chat avec le payload
   - Utilise un AbortController pour permettre l'interruption
   - Lit le ReadableStream ligne par ligne
   - Parse chaque ligne "data: {json}" → appelle le handler correspondant :
     handlers.onConversation(id, isNew)
     handlers.onChunk(text)
     handlers.onTitle(title)
     handlers.onFileReady(url, name, format, size)
     handlers.onDone(messageId)
     handlers.onError(message)

NE construis PAS les composants visuels dans ce prompt.
```

**Critères de succès :**
- Les stores compilent en TypeScript strict sans erreur
- api.ts appelle chaque endpoint correctement (vérifier avec un console.log)
- stream.ts parse un flux SSE simulé et appelle les bons handlers
- chatStore.sendMessage ouvre le flux, concatène les chunks, finalise le message
- chatStore.stopStreaming interrompt le flux proprement

---

### PROMPT 8 — Zone de chat : bulles + Markdown streaming + code

```
En te basant sur CLAUDE.md et le chatStore, construis la zone centrale du chat.

1. Installe les dépendances nécessaires pour le rendu Markdown :
   - react-markdown (ou une alternative streaming-compatible)
   - remark-gfm (tables, strikethrough, etc.)
   - rehype-highlight ou shiki (coloration syntaxique)
   - rehype-raw (HTML dans le Markdown si nécessaire)

2. components/chat/MessageBubble.tsx :
   - Bulle UTILISATEUR : alignée à droite, fond marine léger (#0B1F3A avec opacité ou bleu foncé), texte clair, rayon 12px. Affiche le texte brut + les fichiers attachés (chips avec nom/taille).
   - Bulle ASSISTANT : alignée à gauche, fond blanc (#FFFFFF), bordure 0.5px #E0E4EC, rayon 12px. Au-dessus : nom du LLM + modèle en texte tertiaire (ex: "Gemini 2.5 Flash"). Contenu : Markdown rendu complet.
   - Le Markdown rendu inclut :
     * Titres (## ##) avec tailles hiérarchiques DM Sans
     * **Gras**, *italique*, ~~barré~~
     * Listes à puces et numérotées avec imbrication
     * `code inline` avec fond gris, police mono
     * Blocs de code (```) avec :
       - Détection automatique du langage
       - Coloration syntaxique (Shiki ou highlight.js)
       - Fond marine sombre, police mono, rayon 8px
       - Bouton "Copier" en haut à droite du bloc
       - Numéros de lignes pour les blocs > 5 lignes
     * Liens cliquables (ouverts dans un nouvel onglet)
     * Emojis rendus nativement (Unicode)
   - Pendant le STREAMING : le Markdown se construit progressivement. Utilise un parser qui gère le Markdown incomplet (syntaxes non fermées). Curseur animé (caret clignotant) en fin de texte.

3. components/chat/ChatWindow.tsx :
   - Affiche la liste des messages de la conversation courante depuis le store.
   - Pendant le streaming : affiche la bulle assistant en cours de construction (streamingText du store, rendu Markdown en temps réel).
   - Auto-scroll vers le bas à chaque nouveau chunk.
   - État vide (pas de messages) : message d'accueil centré ("Que puis-je faire pour vous ?" ou similaire, style charte).
   - Utilise les messages et isStreaming du chatStore.

4. Intègre ChatWindow dans app/chat/page.tsx et app/chat/[id]/page.tsx.
   - page.tsx : chat vide (nouvelle conversation)
   - [id]/page.tsx : charge la conversation par id au montage (loadConversation)

Concentre-toi sur la QUALITÉ du rendu Markdown et du streaming. C'est le composant le plus visible de toute la plateforme.
```

**Critères de succès :**
- Envoyer un message → la réponse streame mot par mot avec curseur animé
- **gras**, *italique*, listes à puces s'affichent correctement PENDANT le streaming
- Un bloc ```python s'affiche avec coloration syntaxique et bouton Copier fonctionnel
- `code inline` a un fond gris et une police mono
- Les emojis s'affichent nativement
- L'auto-scroll suit la réponse en cours
- L'état vide affiche un message d'accueil
- /chat/{id} charge et affiche une conversation existante

---

### PROMPT 9 — Sidebar conversations + sélecteur LLM + input

```
En te basant sur CLAUDE.md et le chatStore, construis la sidebar, le sélecteur et l'input.

1. components/sidebar-left/ConversationList.tsx :
   - Bouton "Nouvelle conversation" en haut (CTA bleu Boulga, icône Tabler plus).
   - Barre de recherche (Input avec icône search, filtre les conversations par titre).
   - Liste des conversations groupées :
     * "Aujourd'hui" — conversations updated_at = aujourd'hui
     * "Hier" — updated_at = hier
     * "7 derniers jours" — updated_at dans les 7 jours
     * "Plus ancien" — le reste
   - Chaque conversation : titre (1 ligne, truncate), date relative.
   - Conversation active : fond bleu léger.
   - Au survol : boutons renommer (icône pencil) et supprimer (icône trash, rouge).
   - Clic → loadConversation(id) + router.push(/chat/{id}).
   - Supprimer → confirmation puis deleteConversation(id).
   - Charge les conversations au montage (loadConversations).
   - Sur MOBILE : ce composant est dans un drawer (slide depuis la gauche), avec un bouton hamburger pour l'ouvrir. Le drawer se ferme après sélection d'une conversation.

2. components/chat/LLMSelector.tsx :
   - Affiché en haut de la zone de chat, aligné à gauche.
   - Dropdown 1 : choix du LLM. Affiche le label + une petite icône/couleur.
     * LLM actifs : cliquables.
     * LLM inactifs : grisés avec badge "Bientôt" — non cliquables.
   - Dropdown 2 : choix du modèle du LLM sélectionné.
     * Chaque modèle affiche : label + badge tier (low="Éco", high="Pro") + description courte.
     * Si l'utilisateur n'a pas le niveau suffisant pour un modèle → grisé avec badge "Business requis".
   - Le modèle économique (low) est sélectionné par défaut.
   - Met à jour selectedProvider et selectedModel dans le store.
   - Charge les LLM au montage (loadLLMs).

3. components/chat/ChatInput.tsx :
   - Zone de saisie multiligne (textarea) :
     * Grandit avec le contenu (min 1 ligne, max ~6 lignes, puis scroll interne).
     * Placeholder : "Décrivez votre besoin, posez une question, uploadez un fichier..."
   - Bouton envoi à droite (icône arrow-up ou send, fond bleu Boulga, rayon pill). Grisé si le texte est vide.
   - Bouton pièce jointe à gauche (icône paperclip). Clic → ouvre un sélecteur de fichier (accept: .csv, .pdf, .docx, .xlsx, .png, .jpg, .txt).
   - Fichiers attachés : affichés comme des chips au-dessus du textarea (nom tronqué + taille + bouton ✕ pour retirer). Stockés dans un état local. À l'envoi, uploadés via uploadFile() puis file_ids envoyés avec le message.
   - Raccourcis clavier : Entrée = envoyer, Shift+Entrée = nouvelle ligne.
   - Pendant le streaming : le textarea est désactivé. Le bouton envoi devient un bouton STOP (icône square, fond rouge). Clic → stopStreaming().
   - Après le streaming : réactive le textarea, focus automatique.

Intègre les 3 composants dans le layout du chat (sidebar + sélecteur + window + input).
```

**Critères de succès :**
- La sidebar liste les conversations groupées par date
- Créer une nouvelle conversation → elle apparaît dans la sidebar après le premier message
- Cliquer sur une conversation → charge l'historique, URL passe à /chat/{id}
- Supprimer → la conversation disparaît (avec confirmation)
- La recherche filtre les conversations par titre
- Le sélecteur affiche Gemini (actif) + 3 LLM grisés "Bientôt"
- Changer de modèle (Flash → Pro) → le prochain message utilise le nouveau modèle
- Attacher un fichier → chip visible, fichier envoyé avec le message
- Entrée = envoie, Shift+Entrée = nouvelle ligne
- Pendant le streaming : input désactivé, bouton Stop visible et fonctionnel
- Sur mobile : la sidebar est un drawer

---

### PROMPT 10 — Tableaux, images, citations, actions, polish

```
En te basant sur CLAUDE.md, complète et peaufine le rendu du chat.

1. TABLEAUX MARKDOWN dans MessageBubble :
   - Installer remark-gfm si pas déjà fait.
   - Les tableaux | col | col | s'affichent avec :
     * En-têtes en gras, fond marine léger
     * Bordures fines #E0E4EC
     * Alternance de couleurs sur les lignes (blanc / gris #F5F5F5)
     * Scroll horizontal si le tableau est trop large

2. IMAGES dans les bulles :
   - Les images dans les réponses (graphiques matplotlib, DALL-E, etc.) s'affichent inline dans la bulle assistant.
   - Max-width 100% du conteneur de la bulle.
   - Clic sur une image → lightbox (overlay sombre, image centrée plein écran, clic/Esc pour fermer).
   - Les images uploadées par l'utilisateur s'affichent en miniature (64x64) dans sa bulle.

3. CITATIONS Markdown :
   - Les blocs > citation s'affichent avec :
     * Bordure gauche 3px bleu Boulga #1565C0
     * Fond très léger #F5F7FA
     * Padding gauche 16px
     * Texte en italique légère

4. ACTIONS sur les bulles assistant :
   - Au survol (desktop) ou au tap long (mobile), afficher 3 boutons :
     * Copier (icône copy) — copie le contenu texte brut dans le presse-papier, feedback visuel "Copié ✓"
     * Régénérer (icône refresh) — renvoie le même message au LLM, remplace la réponse
     * 👍 / 👎 (feedback) — un clic enregistre le feedback (store le rating, pas encore envoyé au backend — sera branché au prompt 14)
   - Boutons petits, discrets (tertiaire), positionnés en bas à droite de la bulle.

5. RESPONSIVE des bulles :
   - Desktop : largeur max des bulles = 70% du conteneur
   - Mobile : largeur max = 90%
   - Les blocs de code ont un scroll horizontal si le code est large

6. État de CHARGEMENT :
   - Pendant le streaming, avant le premier chunk : afficher un indicateur "thinking" (3 points animés ou skeleton pulse) dans la bulle assistant.
```

**Critères de succès :**
- Un tableau Markdown s'affiche correctement avec en-têtes, bordures, alternance
- Une image dans une réponse s'affiche inline, clic → lightbox plein écran
- Les citations > ont le bon style (bordure gauche bleue)
- Copier → texte dans le presse-papier, feedback visuel
- Régénérer → le même message est renvoyé, nouvelle réponse
- 👍/👎 → feedback visuel (le bouton change de couleur)
- Les bulles sont max 70% desktop, 90% mobile
- L'indicateur "thinking" s'affiche avant le premier chunk

---

## PHASE 4 — DOCUMENTS

### PROMPT 11 — Panneau document + génération + téléchargement

```
En te basant sur CLAUDE.md, construis le panneau document et la génération de fichiers.

1. frontend/store/docStore.ts (Zustand) :
   État :
     currentDocument: { content: string, format: string, versions: string[] } | null
     fileReady: { url: string, name: string, format: string, size: number } | null
   Actions :
     openDocument(content) — ouvre le panneau avec le contenu
     updateDocument(content) — met à jour (nouvelle version, ancienne version ajoutée à versions)
     setFileReady(info) — fichier prêt à télécharger
     closeDocument() — ferme le panneau
     goToVersion(index) — navigue dans l'historique

2. components/sidebar-right/DocumentPanel.tsx :
   - Le panneau occupe la colonne droite (400px desktop) ou le plein écran (mobile).
   - Il s'ouvre quand le LLM produit un contenu long (détection : réponse > 500 mots ou événement file_ready).
   - En-tête : titre "Document" + bouton fermer (✕).
   - Corps : rendu du contenu selon le type :
     * Markdown → rendu riche (même composants que MessageBubble mais sans la bulle)
     * Code → coloration syntaxique plein panneau
     * Fichier généré → icône du format + nom + taille
   - Barre d'actions en bas :
     * Bouton Copier (copie le contenu brut)
     * Bouton Télécharger :
       - Si un fichier est prêt (file_ready) → télécharge le fichier via api.downloadFile
       - Sinon → télécharge le contenu en .md ou .txt
     * Bouton Versions (si > 1 version) → dropdown avec la liste, clic → goToVersion
   - Sur mobile : le panneau s'ouvre en plein écran avec bouton retour (← Retour au chat).

3. Intégration dans le flux SSE :
   - Dans chatStore.sendMessage, quand l'événement file_ready arrive :
     * docStore.setFileReady(info) → le panneau s'ouvre avec le bouton Télécharger
   - Quand la réponse est longue (> 500 mots) :
     * docStore.openDocument(content) → le panneau s'ouvre avec le contenu rendu
   - Quand l'utilisateur demande une modification et que le LLM régénère :
     * docStore.updateDocument(newContent) → nouvelle version, panneau mis à jour

4. La page Documents (app/documents/page.tsx) :
   - Branche maintenant sur les vrais données : GET /api/files récupère la liste des fichiers de l'utilisateur.
   - Chaque fichier : nom, format (badge), date, taille, bouton Télécharger.
   - État vide si aucun fichier.

5. Ajout endpoint backend :
   - GET /api/files : liste les fichiers de l'utilisateur (file_repository.list_for_user).
   Enregistre dans le router files.
```

**Critères de succès :**
- Demander un texte long → le panneau document s'ouvre avec le contenu rendu
- Demander un fichier Word → file_ready → bouton Télécharger → fichier réel téléchargé
- Modifier le document ("change l'introduction") → le panneau se met à jour, version précédente accessible
- Bouton Copier → contenu dans le presse-papier
- Bouton Versions → navigation entre les versions
- La page Documents liste les fichiers avec téléchargement
- Sur mobile → panneau en plein écran avec bouton retour

---

## PHASE 4B — FEATURES DIFFÉRENCIANTES

### PROMPT 11B — Mode Comparaison

```
En te basant sur CLAUDE.md (section Features différenciantes), construis le Mode Comparaison.

BACKEND :
1. app/services/compare_service.py — CompareService :
   - compare(user_id, prompt, providers_models, file_ids) :
     * Vérifie que l'utilisateur est au moins Source (sinon erreur 403)
     * Crée une comparison_session en base
     * Lance les appels LLM EN PARALLÈLE (asyncio.gather) pour chaque provider/modèle demandé
     * Pour chaque réponse : enregistre dans comparison_results (content, tokens_used, latency_ms)
     * Retourne un AsyncIterator SSE avec des événements par provider :
       {"type": "compare_chunk", "provider": "gemini", "model": "gemini-2.5-flash", "text": "..."}
       {"type": "compare_done", "provider": "gemini", "latency_ms": 1240, "tokens": 320}
       {"type": "all_done", "session_id": "..."}
     * Chaque provider streame indépendamment — si l'un timeout (>30s), il émet une erreur partielle
       sans bloquer les autres

2. app/routers/compare.py :
   - POST /api/compare :
     * Body : { prompt, providers: [{provider, model_id}], file_ids? }
     * Vérifie le tier (source+)
     * Retourne StreamingResponse SSE (même format que /api/chat)
   - GET /api/compare/history : liste les sessions de comparaison de l'utilisateur
   - GET /api/compare/{session_id} : résultats complets d'une session

FRONTEND :
3. frontend/store/compareStore.ts (Zustand) :
   État : sessions[], currentSession, results: { [provider]: string }, isComparing, activeProviders
   Actions : startComparison(prompt, providers), stopComparison, loadHistory

4. app/compare/page.tsx — page Mode Comparaison :
   - En-tête : titre "Mode Comparaison" + description courte
   - Sélecteur multi-provider : checkboxes pour choisir 2 à 4 LLM à comparer
     * Seuls les LLM actifs et accessibles selon le tier sont cochables
   - Zone de saisie (même ChatInput que le chat normal)
   - Zone de résultats en colonnes :
     DESKTOP : grille côte à côte (2 à 4 colonnes selon le nombre de LLM sélectionnés)
     MOBILE  : onglets (un onglet par LLM)
   - Chaque colonne : nom du LLM + modèle en en-tête, réponse en Markdown streaming en temps réel
   - Badge latence en ms sur chaque colonne une fois terminé
   - Badge tokens utilisés par colonne
   - Si un LLM timeout ou erreur → colonne grisée avec message d'erreur, les autres continuent
   - Bouton "Copier tout" → copie toutes les réponses formatées
   - Bouton "Sauvegarder" → enregistre la session pour la retrouver dans l'historique

5. Lien vers le Mode Comparaison :
   - Bouton dans la navigation (icône columns ou layout-columns Tabler) visible pour Source+
   - Pour les users Gratuit/Goutte : icône grisée avec tooltip "Disponible à partir de Source"
```

**Critères de succès :**
- Envoyer un prompt → 2-4 LLM streamant simultanément dans leurs colonnes
- Si Gemini répond en 1s et Claude en 3s → Gemini affiche sa réponse complète pendant que Claude streame encore
- Un LLM en timeout → sa colonne affiche l'erreur, les autres ne sont pas bloqués
- Les badges latence et tokens s'affichent correctement après chaque réponse
- Un user Goutte accède à /compare → redirection avec message "Source requis"
- L'historique des comparaisons est accessible

---

### PROMPT 11C — Routage Automatique + Bot WhatsApp

```
En te basant sur CLAUDE.md, construis le routage automatique et le bot WhatsApp.

ROUTAGE AUTOMATIQUE :
1. app/manager/router_agent.py :
   - Classe RouterAgent avec méthode route(prompt, user_tier) → { provider, model_id, reason }
   - Logique de routage basée sur des règles + appel LLM léger (Gemini Flash) pour classification :
     * Mots-clés code/développement → deepseek-v4-flash
     * Analyse/juridique/raisonnement complexe → claude-haiku-4-5 (ou sonnet si fleuve+)
     * Créativité/rédaction/marketing → gemini-2.5-flash
     * Fichier long/PDF/recherche → gemini-2.5-flash
     * Par défaut → gemini-2.5-flash
   - Ne route que vers des providers actifs et accessibles selon le tier de l'utilisateur
   - Retourne aussi une "reason" courte pour l'afficher dans l'UI (ex: "Code détecté → DeepSeek")

2. app/prompts/routing_prompts.py :
   - ROUTING_PROMPT : prompt système court pour classifier la requête (réponse JSON : provider + reason)

3. Intégration dans ChatService :
   - Si l'utilisateur active le mode "Boulga choisit" (flag auto_route=true dans ChatRequest)
     → appeler router_agent.route() avant l'appel LLM principal
     → yield {"type": "routing", "provider": "deepseek", "model": "deepseek-v4-flash", "reason": "..."}
     → utiliser le provider/modèle suggéré pour le streaming

4. Frontend — RoutingIndicator.tsx :
   - Petit bandeau qui s'affiche avant le premier chunk quand le routage automatique est actif
   - Affiche : "Boulga a choisi [icône LLM] [nom modèle] — [raison]"
   - Disparaît après 3 secondes ou au clic
   - Toggle dans ChatInput : icône "baguette magique" pour activer/désactiver le routage auto
   - Disponible à partir de Source

BOT WHATSAPP :
5. app/services/whatsapp_service.py :
   - verify_webhook(token) → vérifie le token de vérification Meta
   - handle_message(phone, message_text, media_url?) :
     * Trouve le user lié au numéro WhatsApp (table whatsapp_sessions)
     * Si non lié : répond avec un lien d'activation "Connectez votre compte Boulga : [lien]"
     * Si lié : appelle ChatService.stream_message avec le provider par défaut de l'utilisateur
     * Envoie la réponse complète via l'API WhatsApp Business (pas de streaming, réponse finale)
   - link_whatsapp(user_id, phone_number) : associe un numéro au compte
   - send_message(phone, text) : envoie un message via l'API Meta WhatsApp Business

6. app/routers/whatsapp.py :
   - GET /api/whatsapp/webhook (vérification Meta)
   - POST /api/whatsapp/webhook (messages entrants)
   - POST /api/whatsapp/link { phone_number } → envoie un SMS de vérification
   - POST /api/whatsapp/verify { code } → confirme et lie le numéro

7. Variables d'environnement à ajouter dans .env.example :
   WHATSAPP_TOKEN, WHATSAPP_PHONE_ID, WHATSAPP_VERIFY_TOKEN, META_APP_SECRET

8. Frontend — Section WhatsApp dans Settings :
   - Pour les users Source+ : section "Bot WhatsApp"
   - Champ numéro de téléphone + bouton "Connecter"
   - Flux : entrer le numéro → recevoir un code par SMS → entrer le code → compte lié
   - Une fois lié : affiche le numéro lié + bouton "Déconnecter"
   - Pour Gratuit/Goutte : section grisée avec "Disponible à partir de Source"
```

**Critères de succès :**
- Envoyer un message avec auto_route=true → bandeau "Boulga a choisi X — raison" s'affiche
- Un message de code Python → DeepSeek sélectionné automatiquement
- POST /api/whatsapp/webhook reçoit un message et retourne une réponse LLM
- Un numéro non lié reçoit le message d'activation
- La liaison WhatsApp fonctionne (vérification par SMS)
- Le toggle routage auto dans l'input est visible pour Source+

---

## PHASE 5 — AUTH + PAIEMENT

### PROMPT 12 — Authentification complète

```
En te basant sur CLAUDE.md, ajoute l'authentification.

BACKEND :
1. app/services/auth_service.py — AuthService :
   - register(email, name, date_of_birth, password) :
     * Vérifie que l'email n'existe pas déjà (→ erreur 409)
     * Hash le mot de passe (bcrypt)
     * Crée l'utilisateur (user_repository)
     * Crée un abonnement Gratuit (subscription_repository)
     * Crée un quota initial (quota_repository)
     * Envoie un email de vérification avec un token (Resend)
     * Retourne l'utilisateur créé
   - login(email, password) :
     * Vérifie les credentials
     * Retourne un JWT + refresh token en cookies httpOnly
   - verify_email(token) : marque email_verified = true
   - forgot_password(email) : envoie un email avec lien de réinitialisation
   - reset_password(token, new_password) : met à jour le mot de passe

2. app/routers/auth.py :
   - POST /api/auth/register — inscription
   - POST /api/auth/login — connexion (set cookies)
   - POST /api/auth/logout — supprime les cookies
   - GET /api/auth/me — profil de l'utilisateur connecté (nécessite JWT)
   - POST /api/auth/verify-email — vérification email
   - POST /api/auth/forgot-password — demande de réinitialisation
   - POST /api/auth/reset-password — réinitialisation

3. Branche le middleware auth :
   - Les routes /api/chat, /api/conversations, /api/files nécessitent un JWT valide.
   - Les routes /api/llms, /api/auth/* sont publiques.
   - Le user_id est extrait du JWT et passé aux services.

FRONTEND :
4. Branche authStore : login/logout/loadUser réels.
5. app/auth/register/page.tsx — formulaire d'inscription :
   - Champs : Nom complet, Email, Date de naissance (date picker), Mot de passe, Confirmer le mot de passe.
   - Validation côté client (email valide, mdp min 8 car, correspondance).
   - Style charte (inputs, boutons, fond neutre).
   - Après inscription → redirection vers /chat avec message "Vérifiez votre email".

6. app/auth/login/page.tsx — formulaire de connexion :
   - Champs : Email, Mot de passe, case "Se souvenir de moi".
   - Lien "Mot de passe oublié" → /auth/forgot.
   - Après connexion → redirection vers /chat.

7. app/auth/forgot/page.tsx — mot de passe oublié :
   - Champ email, bouton envoyer, message de confirmation.

8. Protection des routes :
   - Middleware ou wrapper qui vérifie si l'utilisateur est connecté.
   - Si non connecté et accès à /chat, /documents, /settings → redirection vers /auth/login.
   - La landing page / et /pricing restent publiques.

9. Page Paramètres (app/settings/page.tsx) : branche sur les vraies données (profil, abonnement, quota depuis l'API).
```

**Critères de succès :**
- S'inscrire avec email/nom/naissance/mdp → compte créé, abonnement Gratuit attribué
- Email de vérification reçu (via Resend, vérifier dans les logs si pas de clé Resend)
- Se connecter → JWT en cookie, accès au chat
- Se déconnecter → cookie supprimé, redirection /auth/login
- Accéder à /chat sans être connecté → redirection /auth/login
- GET /api/auth/me retourne le profil de l'utilisateur connecté
- Les conversations sont liées au vrai user_id
- Mot de passe oublié → email reçu → réinitialisation fonctionne
- Page Paramètres affiche les vraies données du profil

---

### PROMPT 13 — Abonnements + quotas + CinetPay

```
En te basant sur CLAUDE.md, ajoute les abonnements, les quotas et le paiement.

Les 5 niveaux d'abonnement (voir CLAUDE.md section Abonnements) :
  free (gratuit), goutte (2 999), source (5 999), fleuve (9 999), ocean (29 999) FCFA/mois
Deux cycles de facturation : monthly | annual (annual = 10 mois payés, 12 mois de service).
Prix annuels : goutte=29 990, source=59 990, fleuve=99 990, ocean=299 990 FCFA.

BACKEND :
1. app/services/subscription_service.py :
   - get_current(user_id) → abonnement actif + quota restant + billing_cycle
   - check_can_send_message(user_id) → bool selon le tier
   - check_can_generate_file(user_id) → bool selon le tier
   - check_can_use_model(user_id, provider, model_id) → bool (vérifie accès LLM/modèle selon tier)
   - check_can_compare(user_id) → bool (source+ seulement)
   - check_can_use_agent(user_id, agent_id) → bool (fleuve: 2 agents max, ocean: tous)
   - activate_subscription(user_id, tier, billing_cycle) → crée/met à jour, réinitialise quota
   - expire_subscriptions() → cron : abonnements expirés → downgrade free

2. app/services/quota_service.py :
   Compteurs Redis (temps réel) + persistance usage_quotas (PostgreSQL).
   - consume_message(user_id) — incrémente messages_used + tokens_used
   - consume_file(user_id) — incrémente files_generated
   - consume_tokens(user_id, count) — incrémente tokens_used (pour l'Océan 50M cap)
   - get_remaining(user_id) → { messages_remaining, files_remaining, tokens_remaining, period_end }
   Limites par tier :
     free    → 10 msg/jour, 0 fichiers, Gemini Flash seul
     goutte  → 600 msg/mois, 3 fichiers/mois
     source  → 800 msg/mois, 20 fichiers/mois
     fleuve  → 2 000 msg/mois, 50 fichiers/mois
     ocean   → illimité messages, illimité fichiers, cap 50M tokens/mois (alerte admin si dépassé)
   Réinitialisation : quotidienne (free), mensuelle (tous les autres).

3. app/services/payment_service.py — CinetPay :
   - initiate_payment(user_id, tier, billing_cycle) :
     * Montants : voir tableau CLAUDE.md section Abonnements
     * Crée payment (status=pending), appelle API CinetPay, retourne URL
   - handle_webhook(payload) :
     * Vérifie signature CinetPay → 403 si invalide
     * Si success → activate_subscription, envoie email reçu (Resend)
     * Si failed → aucun changement
   - check_payment_status(payment_id) : polling fallback

4. app/routers/payments.py :
   - POST /api/payments/initiate { tier, billing_cycle } → { payment_url }
   - POST /api/payments/webhook (callback CinetPay)

5. app/routers/subscriptions.py :
   - GET /api/subscriptions/me → { tier, billing_cycle, status, expires_at,
                                    messages_remaining, files_remaining, tokens_remaining }

6. Quotas dans ChatService :
   - AVANT LLM : check_can_send_message + check_can_use_model. Si false → SSE error.
   - AVANT Mode Comparaison : check_can_compare. Si false → erreur "Disponible à partir de Source".
   - APRÈS réponse : consume_message + consume_tokens.

7. Agents dans le backend :
   - Les agents (table agents) sont pré-chargés via un script seed.
   - GET /api/agents → liste tous les agents actifs
   - POST /api/agents/{id}/assign → assigne à l'utilisateur (vérifie limite Fleuve : 2 max)
   - DELETE /api/agents/{id}/unassign → désassigne

FRONTEND :
8. Page Pricing :
   - Toggle Mensuel / Annuel en haut → les prix se mettent à jour sur toutes les cards.
   - 5 cards : Gratuit, Goutte, Source (badge "Populaire"), Fleuve, Océan.
   - Sur chaque card payante : bouton "Choisir ce plan" → initiate_payment → redirect CinetPay.
   - Card Gratuit : bouton "Commencer gratuitement" → /auth/register.
   - Afficher l'économie annuelle sur chaque card : "Économisez X FCFA".
   - Page de confirmation retour CinetPay : vérifie le statut, affiche succès ou échec.

9. Quota dans l'interface :
   - Badge compact (tier + messages restants) en haut de la zone chat.
   - Quota à 80% → toast orange "Vous approchez de votre limite".
   - Quota épuisé → modal bloquant avec bouton "Voir les offres" → /pricing.

10. LLM grisés selon tier :
    - Sélecteur LLM : LLM/modèles non accessibles grisés avec badge du tier requis.
    - Si free sélectionne Claude Sonnet → tooltip "Disponible à partir de Source".

11. Agents dans les paramètres (page settings) :
    - Section "Mes Agents" visible pour Fleuve et Océan.
    - Fleuve : affiche les 8 agents avec 2 slots sélectionnables.
    - Océan : affiche les 8 agents tous actifs + bouton "Créer un agent personnalisé".

Enregistre tous les routers dans main.py.
Variables d'environnement CinetPay en mode sandbox pour les tests.
```

**Critères de succès :**
- Un user Gratuit est limité à 10 messages/jour → bloqué au 11e AVANT l'appel LLM
- Un user Goutte sélectionne Claude Sonnet → "Disponible à partir de Source"
- Toggle Mensuel/Annuel → les prix s'affichent correctement sur la page pricing
- POST /api/payments/initiate avec billing_cycle=annual retourne URL CinetPay (mode test)
- Le webhook CinetPay active l'abonnement et envoie l'email de confirmation
- GET /api/subscriptions/me retourne le tier + billing_cycle + quotas corrects
- Un user Fleuve peut assigner 2 agents max → le 3e est bloqué
- Un user Océan voit les 8 agents actifs dans ses paramètres
- Badge quota visible, toast orange à 80%, modal bloquant à 0%
- Un abonnement expiré → retour Gratuit automatique, toast informatif

---

## PHASE 6 — FINITION

### PROMPT 13B — Programme de parrainage

```
En te basant sur CLAUDE.md (section Programme de parrainage), construis le système de parrainage.

BACKEND :
1. Champ referral_code sur la table users :
   - Généré automatiquement à l'inscription (ex: BOULGA-A3X9K, 8 caractères uniques)
   - Via app/core/security.py : fonction generate_referral_code()

2. Table referrals : referrer_id, referred_id, status, reward_granted_at, created_at.

3. app/services/referral_service.py — ReferralService :
   - get_referral_link(user_id) → URL complète : https://boulga.ai/register?ref=CODE
   - register_with_referral(referred_user_id, referral_code) :
     * Vérifie que le code existe et que ce n'est pas un auto-parrainage (même email/IP)
     * Trouve le referrer via referral_code
     * Crée un enregistrement referrals (status=pending)
   - schedule_reward(referrer_id, referred_tier) :
     * Appelé lors du premier paiement réussi du filleul
     * Enregistre la récompense en attente avec reward_due_at = now + 7 jours
     * Calcule la récompense selon le tier du filleul :
         goutte  → 14 jours Goutte
         source  → 30 jours Goutte
         fleuve  → 30 jours Source
         ocean   → 30 jours Fleuve
     * Stocke reward_tier et reward_days dans referrals
   - process_pending_rewards() :
     * Cron quotidien : trouve les referrals où reward_due_at <= now et status=pending
     * Vérifie que le paiement du filleul n'a pas été remboursé (status toujours success)
     * Si OK → appelle grant_reward()
     * Si remboursé → passe status=cancelled, n'accorde rien
   - grant_reward(referral_id) :
     * Si referrer sur niveau < reward_tier → monte temporairement au reward_tier
     * Si referrer sur niveau >= reward_tier → ajoute reward_days à expires_at
     * Met à jour referrals (status=completed, reward_granted_at=now)
     * Notifie le referrer (WhatsApp si lié, email sinon)
   - get_stats(user_id) → { total_referrals, completed, pending, cancelled, days_earned }

4. Intégration dans payment_service.py :
   - Dans handle_webhook(), après activation de l'abonnement :
     * Vérifie si le user a été parrainé (referrals WHERE referred_id=user_id AND status=pending)
     * Si oui et c'est son premier paiement → appelle referral_service.grant_reward(referrer_id)

5. app/routers/referrals.py :
   - GET /api/referrals/link → retourne { referral_code, referral_url }
   - GET /api/referrals/stats → { total, completed, days_earned }
   - POST /api/referrals/invite { email } → envoie un email d'invitation (Resend)

FRONTEND :
6. Section "Parrainage" dans app/settings/page.tsx :
   - Titre : "Invitez vos amis, gagnez des semaines gratuites"
   - Description : "Chaque ami qui souscrit un abonnement payant vous rapporte une récompense.
     Goutte → 14j Goutte | Source → 30j Goutte | Fleuve → 30j Source | Océan → 30j Fleuve."
   - Lien de parrainage : input en lecture seule avec le lien + bouton "Copier"
   - Bouton principal : "Partager sur WhatsApp" (fond vert WhatsApp #25D366)
     → ouvre wa.me avec message pré-rédigé :
     "Salut ! J'utilise Boulga AI pour accéder à tous les LLM en un seul endroit.
      Rejoins-moi avec mon lien : [lien]"
   - Bouton secondaire : "Inviter par email" → champ email + bouton Envoyer
   - Compteur : "X amis invités · Y abonnés · Z jours gagnés"
   - Historique compact : liste des filleuls (prénom masqué + date + tier souscrit + statut pending/complété)

7. À l'inscription (app/auth/register/page.tsx) :
   - Si l'URL contient ?ref=CODE → stocker le code dans un cookie httpOnly (pas localStorage)
   - Après création du compte → appeler register_with_referral avec le code stocké
   - Afficher un message : "Bienvenue sur Boulga ! Vous avez rejoint la plateforme via un lien d'invitation."

8. Notification de récompense (pour le parrain uniquement) :
   - WhatsApp (si lié) : "🎉 Votre filleul vient de souscrire à Boulga !
     Vous avez gagné [X jours] [Tier] gratuit. Votre accès expire le [date]."
   - Email (fallback via Resend) : même message, format HTML avec le design Boulga.
```

**Critères de succès :**
- Chaque utilisateur a un lien unique /register?ref=CODE
- S'inscrire via un lien de parrainage → referral créé (status=pending)
- Filleul souscrit Goutte → parrain reçoit 14 jours Goutte après 7 jours
- Filleul souscrit Source → parrain reçoit 1 mois Goutte après 7 jours
- Filleul souscrit Fleuve → parrain reçoit 1 mois Source après 7 jours
- Filleul souscrit Océan → parrain reçoit 1 mois Fleuve après 7 jours
- Si filleul remboursé dans les 7 jours → récompense annulée, parrain ne reçoit rien
- Un user Gratuit parrainé → monte temporairement au niveau récompense → retour Gratuit à expiration
- Notification WhatsApp si compte lié, email sinon — envoyée le jour du déblocage de la récompense
- Bouton "Partager sur WhatsApp" ouvre WhatsApp avec message pré-rédigé
- Pas de double récompense si le filleul paie plusieurs fois
- Auto-parrainage (même email/IP) → bloqué à l'inscription
- Cron quotidien process_pending_rewards() fonctionne sans erreur

---

### PROMPT 14 — Notifications + feedback + recherche

```
En te basant sur CLAUDE.md, ajoute les notifications, le feedback et la recherche.

NOTIFICATIONS :
1. Système de toast in-app :
   - Composant Toast déjà créé (prompt 2). L'utiliser pour :
     * Fichier prêt → toast "Votre fichier est prêt" (bleu info)
     * Paiement confirmé → toast "Abonnement [Tier] activé !" (vert succès)
     * Quota à 80% → toast "Vous approchez de votre limite" (orange attention)
     * Récompense de parrainage → toast "Vous avez gagné X jours [Tier] gratuit !" (vert succès)
   - Les toasts disparaissent après 5 secondes ou clic sur ✕.

2. Modal bloquant :
   - Quota épuisé → modal centré, overlay sombre :
     "Vous avez atteint votre limite de messages."
     "Passez au niveau Goutte ou supérieur pour continuer."
     Bouton "Voir les offres" → /pricing
     Bouton "Fermer" (texte ghost)

3. Notifications email (Resend) :
   - Paiement confirmé → email avec reçu (montant, date, niveau activé, cycle mensuel/annuel)
   - Abonnement expire dans 3 jours → email de rappel avec lien /pricing
   - Récompense de parrainage accordée → email "Votre ami a souscrit ! Vous bénéficiez de X jours [Tier] gratuit"
   - Implémente une tâche planifiable pour le rappel (endpoint interne ou commentaire "TODO: cron").

FEEDBACK :
4. Backend : app/routers/feedback.py
   - POST /api/feedback { message_id, rating: "up"|"down", comment? }
   - Enregistre via feedback_repository. Retourne 201.

5. Frontend : branche les boutons 👍/👎 du prompt 10 sur le backend :
   - Clic 👍 → POST /api/feedback { rating: "up" }. Le bouton passe en bleu.
   - Clic 👎 → affiche un champ "Qu'est-ce qui n'allait pas ?" (textarea 280 car max) + bouton Envoyer. POST /api/feedback { rating: "down", comment }.
   - Un seul feedback par message (si déjà noté, les boutons sont désactivés).

RECHERCHE :
6. Backend : app/routers/search.py
   - GET /api/search?q=terme → recherche plein texte dans messages.content (PostgreSQL tsvector/to_tsquery ou ILIKE pour commencer).
   - Retourne les conversations contenant le terme, avec un extrait du message pertinent.

7. Frontend : la barre de recherche dans la sidebar gauche (déjà créée au prompt 9) :
   - Quand l'utilisateur tape et valide (Entrée ou debounce 500ms) → appelle /api/search.
   - Les résultats remplacent la liste des conversations.
   - Chaque résultat : titre de la conversation + extrait du message + date.
   - Clic → ouvre la conversation.
   - Effacer la recherche → revient à la liste normale.

Enregistre les routers dans main.py.
```

**Critères de succès :**
- Toast "fichier prêt" s'affiche et disparaît après 5s
- Modal quota épuisé s'affiche avec les bons boutons
- 👎 → champ commentaire → feedback stocké en base
- 👍 → feedback stocké, bouton visuellement activé
- Rechercher "rapport" → conversations contenant ce mot listées avec extraits
- Clic sur un résultat → ouvre la conversation au bon endroit

---

### PROMPT 15 — Responsive mobile complet

```
En te basant sur CLAUDE.md, vérifie et corrige le responsive de toute la plateforme.

Teste CHAQUE page et composant sur un viewport de 375px (iPhone SE) et 390px (iPhone 14).

1. NAVIGATION :
   - La sidebar icônes (desktop) disparaît sur mobile.
   - Les onglets en bas apparaissent : Hub, Comparaison (visible si Source+ uniquement), Documents, Paramètres.
     Sur mobile, afficher 3 onglets maximum + menu "Plus" si nécessaire.
   - L'onglet actif est bleu Boulga.

2. CHAT :
   - Sidebar conversations : drawer depuis la gauche, bouton hamburger en haut à gauche pour l'ouvrir. Se ferme après sélection.
   - Sélecteur LLM : compact, même position (haut du chat).
   - Zone de messages : plein écran, bulles max 90% de largeur.
   - Input : adapté au clavier mobile (le contenu remonte quand le clavier s'ouvre). Le bouton envoi et le bouton pièce jointe sont toujours visibles.
   - Blocs de code : scroll horizontal si le code est trop large.

3. PANNEAU DOCUMENT :
   - S'ouvre en PLEIN ÉCRAN sur mobile (pas en colonne).
   - Bouton "← Retour au chat" en haut.
   - Boutons Copier / Télécharger / Versions toujours visibles.

4. PAGES GLOBALES :
   - Landing page : hero empilé (pas côte à côte), cards pricing empilées, images responsive.
   - Page auth : formulaires centrés, pleine largeur.
   - Page Paramètres : sections empilées.
   - Page Documents : liste en colonne.

5. INTERACTIONS TACTILES :
   - Les actions au survol (hover) sur les bulles → deviennent tap long sur mobile.
   - Les dropdowns (sélecteur LLM) → utilisent des bottom sheets ou des modals sur mobile plutôt que des dropdowns.
   - Taille minimum des boutons tactiles : 44x44px.

6. PWA (Progressive Web App) :
   - Crée public/manifest.json :
     {
       "name": "Boulga AI",
       "short_name": "Boulga",
       "description": "Puisez l'intelligence qu'il vous faut",
       "start_url": "/chat",
       "display": "standalone",
       "background_color": "#0B1F3A",
       "theme_color": "#1565C0",
       "icons": [
         { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
         { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
       ]
     }
   - Crée public/sw.js (service worker minimal) :
     * Cache les assets statiques (JS, CSS, fonts)
     * Stratégie : network-first pour les appels API, cache-first pour les assets
     * Affiche un écran "Vous êtes hors ligne" si réseau indisponible et pas de cache
   - Enregistre le service worker dans app/layout.tsx (côté client, useEffect)
   - Dans next.config.js : ajoute les headers manifest et thème couleur.
   - Crée les icônes icon-192.png et icon-512.png (logo Boulga sur fond marine #0B1F3A).
   - Sur mobile, le navigateur doit proposer "Ajouter à l'écran d'accueil".

Utilise les breakpoints Tailwind : sm (640), md (768), lg (1024).
```

**Critères de succès :**
- Toutes les pages sont utilisables sur 375px sans scroll horizontal
- La navigation en onglets fonctionne sur mobile
- Le drawer conversations s'ouvre et se ferme proprement
- Le chat est entièrement fonctionnel sur mobile (envoyer, recevoir, streaming)
- Le panneau document s'affiche en plein écran avec bouton retour
- Le clavier mobile ne cache pas l'input
- Les boutons sont assez grands pour le tactile (44px min)
- Lighthouse PWA score ≥ 90 : manifest valide, service worker enregistré
- Sur mobile Chrome : bandeau "Ajouter à l'écran d'accueil" s'affiche
- En mode hors ligne : la page d'accueil et le chat récent restent accessibles depuis le cache

---

### PROMPT 16 — Sécurité + gestion des erreurs

```
En te basant sur CLAUDE.md et le cahier des charges (section cas limites), ajoute la sécurité et gère tous les cas d'erreur.

SÉCURITÉ BACKEND :
1. Rate limiting avec Redis :
   - 100 requêtes/minute par IP sur tous les endpoints.
   - 20 requêtes/minute par user sur POST /api/chat et POST /api/compare.
   - Retourne 429 Too Many Requests avec le header Retry-After.
   Utilise un middleware FastAPI ou un décorateur.

2. CORS strict :
   - Seuls les domaines dans CORS_ORIGINS sont autorisés.
   - Pas de wildcard * en production.

3. Protection webhooks :
   - POST /api/payments/webhook : vérifie la signature CinetPay avant tout traitement.
     Si signature invalide → 403 Forbidden.
   - POST /api/whatsapp/webhook : vérifie le X-Hub-Signature-256 avec META_APP_SECRET (HMAC-SHA256).
     Si signature invalide → 403 Forbidden. Ne jamais logguer le payload brut (données personnelles).

4. Validation des entrées :
   - Tous les inputs sont validés par Pydantic (déjà fait via les schemas).
   - Le prompt système est fixe côté serveur — le user ne peut pas l'injecter ou le modifier via l'API.

5. Headers de sécurité :
   - X-Content-Type-Options: nosniff
   - X-Frame-Options: DENY
   - Strict-Transport-Security (HSTS)

GESTION DES ERREURS FRONTEND :
6. Pour CHAQUE cas limite du cahier des charges, implémente le comportement attendu :

   - Coupure réseau pendant streaming :
     * Détecte l'erreur réseau dans stream.ts
     * Affiche un toast "Connexion perdue"
     * Bouton "Relancer" apparaît dans la bulle assistant partielle
     * Le message partiel est conservé

   - Session expirée (JWT expiré) :
     * L'API retourne 401
     * Le frontend redirige vers /auth/login
     * La conversation en cours est sauvegardée (elle est déjà en base)
     * Toast "Session expirée, reconnectez-vous"

   - Provider LLM en panne :
     * L'événement SSE error contient le message
     * Affiche dans la bulle : "Ce modèle est temporairement indisponible. Essayez un autre modèle."
     * Le sélecteur LLM reste actif pour changer

   - Email non vérifié :
     * Un bandeau jaune (warning) en haut de l'interface : "Vérifiez votre email pour profiter de toutes les fonctionnalités"
     * Bouton "Renvoyer l'email" dans le bandeau
     * L'utilisateur peut quand même utiliser le chat (pas bloquant)

   - Message vide :
     * Le bouton envoi est grisé si le textarea est vide (déjà fait)

   - Erreur serveur 500 :
     * Toast rouge "Une erreur est survenue. Réessayez."
     * Le message utilisateur est conservé dans l'input

7. Page d'erreur globale :
   - app/error.tsx (Next.js error boundary) : message d'erreur propre, bouton "Retour à l'accueil".
   - app/not-found.tsx : page 404 propre.
```

**Critères de succès :**
- Rate limit : bombarder un endpoint → 429 après 100 requêtes/minute
- Webhook avec mauvaise signature → 403
- Coupure réseau simulée (DevTools offline) → toast + bouton Relancer
- JWT expiré → redirection /auth/login avec toast
- Email non vérifié → bandeau jaune visible
- Page 404 → design propre avec lien retour
- Erreur 500 → toast rouge, message conservé

---

### PROMPT 17 — Tests + déploiement + vérification finale

```
En te basant sur CLAUDE.md, ajoute les tests essentiels, configure le déploiement, et vérifie le flux complet.

TESTS BACKEND (pytest) :
1. Installe pytest, pytest-asyncio, httpx (pour TestClient).
2. Crée backend/tests/ avec :
   - test_health.py : GET / et /health retournent 200
   - test_llms.py : GET /api/llms retourne 4 LLM, 1 actif
   - test_auth.py : register → login → me → logout (flux complet)
   - test_chat.py : envoyer un message → recevoir un streaming (mock le LLM si pas de clé API)
   - test_conversations.py : créer → lister → détail → supprimer
   - test_files.py : upload → lister → download
   - test_quota.py : envoyer 10 messages en free → le 11e est bloqué (quota free = 10/jour)
   - test_payment.py : initiate monthly + annual → webhook success → subscription activée ;
                       vérifier que billing_cycle=annual calcule 12 mois de service
   - test_compare.py : POST /api/compare avec 2 providers → recevoir 2 flux SSE simultanés (mock LLM) ;
                       user Gratuit → erreur 403 "Disponible à partir de Source"
   - test_agents.py : assigner agent à user Fleuve → 2e assignation OK → 3e bloquée ;
                      user Océan → tous les agents accessibles
   - test_referral.py : inscrire avec ref=CODE → referral created ;
                        paiement filleul Goutte → reward_due_at = now+7j ;
                        process_pending_rewards() → récompense accordée ;
                        auto-parrainage (même email) → bloqué
   - test_whatsapp.py : GET /webhook (vérification Meta) → 200 avec challenge ;
                        POST /webhook message entrant → réponse LLM envoyée (mock)
   - test_routing.py : message "écris un code Python" avec auto_route=true →
                       provider retourné = deepseek (mock le selector)
3. Un conftest.py avec les fixtures (client de test, user de test par tier, cleanup).

TESTS FRONTEND (vitest ou jest) :
4. Au minimum :
   - test que les stores compilent et que les actions principales fonctionnent (mock API)
   - test que les pages principales se rendent sans erreur

DÉPLOIEMENT :
5. Configuration Vercel (frontend) :
   - vercel.json si nécessaire
   - Variables d'environnement : NEXT_PUBLIC_API_URL, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY

6. Configuration Railway (backend) :
   - Procfile ou railway.toml : web: uvicorn main:app --host 0.0.0.0 --port $PORT
   - Variables d'environnement : toutes les clés de .env.example

7. GitHub Actions — CI pipeline :
   - .github/workflows/ci.yml :
     * Sur push et PR : lint (ruff pour Python, eslint pour TS) + tests backend + tests frontend
     * Si main : déploiement auto (optionnel — peut être fait manuellement au début)

VÉRIFICATION FINALE :
8. Vérifie le flux complet de bout en bout :
   a. Arriver sur la landing page → voir la présentation (5 tiers tarifaires, 4 LLM, missions Boulga)
   b. Cliquer "Commencer gratuitement" → page d'inscription
   c. S'inscrire (avec un lien de parrainage ?ref=CODE si disponible) → compte créé, redirection /chat
   d. Voir le chat vide avec message d'accueil
   e. Sélectionner Gemini 2.5 Flash dans le sélecteur LLM
   f. Envoyer "Bonjour" → réponse en streaming, titre auto-généré
   g. La conversation apparaît dans la sidebar groupée "Aujourd'hui"
   h. Uploader un CSV → le LLM l'analyse et répond sur les données
   i. Demander un document long → le panneau document s'ouvre automatiquement
   j. Demander le fichier Word → téléchargement réel du .docx
   k. Le document apparaît dans la page /documents avec bouton Télécharger
   l. Vérifier le quota (10 messages max/jour en Gratuit) → le 11e est bloqué, modal s'affiche
   m. Aller sur /pricing → toggle Mensuel/Annuel → prix se mettent à jour ;
      cliquer "Choisir Goutte" ou "Choisir Source" → redirection URL CinetPay (mode sandbox)
   n. Activer le Routage Automatique → envoyer un message de code → bandeau "Boulga a choisi X"
   o. Page /compare (si Source+) → envoyer un prompt → 2 colonnes streamant en parallèle
   p. Page Paramètres → profil, abonnement (tier + cycle), quota, section WhatsApp, section Parrainage
   q. Se déconnecter → cookie supprimé, redirection /auth/login
   r. Tester sur mobile (375px) → chat, drawer, comparaison, panneau document, onglets bas
   s. Installer la PWA → icône apparaît sur l'écran d'accueil, s'ouvre en mode standalone

Documente toute anomalie trouvée et corrige-la.
```

**Critères de succès :**
- `pytest` passe sur tous les tests backend (y compris compare, agents, referral, whatsapp, routing)
- `npm test` passe sur les tests frontend
- Le pipeline GitHub Actions est vert (lint + tests)
- Le flux complet (a → s) fonctionne sans erreur
- Le déploiement est configuré pour Vercel + Railway (même si pas encore déployé en production)
- test_quota.py : le 11e message en free est bloqué (pas le 6e)
- test_payment.py : billing_cycle=annual → 12 mois de service calculés correctement
- test_compare.py : user Gratuit sur /api/compare → 403
- test_agents.py : user Fleuve bloqué au 3e agent
- test_referral.py : auto-parrainage bloqué, reward_due_at correctement calculé
- PWA : Lighthouse score ≥ 90, manifest valide

---

## Récapitulatif

| # | Prompt | Phase | Résultat principal |
|---|--------|-------|--------------------|
| 0 | CLAUDE.md | Contexte | Fichier de référence permanent |
| 1 | Setup + DB + repos | Fondations | Projet initialisé, 14 tables, repositories |
| 2 | Design system + navigation | Fondations | Charte appliquée, navigation globale |
| 3 | Landing + pages globales | Fondations | Toutes les pages visibles |
| 4 | Registre LLM + Gemini | Chat backend | Gemini fonctionnel, sélecteur alimenté |
| 5 | Chat streaming + conversations | Chat backend | Chat complet avec SSE |
| 6 | Upload fichiers | Chat backend | Fichiers transmis au LLM |
| 7 | Store + API + SSE | Chat frontend | Couche données prête |
| 8 | Bulles + Markdown + code | Chat frontend | Rendu riche en streaming |
| 9 | Sidebar + sélecteur + input | Chat frontend | Interface chat complète |
| 10 | Tableaux + images + polish | Chat frontend | Rendu finalisé |
| 11 | Panneau document + fichiers | Documents | Artifacts + génération + téléchargement |
| 11B | Mode Comparaison | Features | 4 LLM côte à côte en streaming |
| 11C | Routage Auto + WhatsApp | Features | Sélection intelligente + bot WhatsApp |
| 12 | Authentification | Auth | Inscription/connexion/session |
| 13 | Abonnements + quotas + CinetPay | Paiement | 5 niveaux + annuel + mobile money |
| 14 | Notifs + feedback + recherche | Finition | Toasts, 👍/👎, recherche full-text |
| 15 | Responsive mobile + PWA | Finition | Mobile 100% fonctionnel + installable |
| 16 | Sécurité + erreurs | Finition | Rate limit, cas limites gérés |
| 17 | Tests + déploiement | Finition | CI/CD, flux complet vérifié |

---

*Boulga — le puits où l'on puise l'intelligence.*
*Prompts Claude Code v3.0 — Juin 2026*
